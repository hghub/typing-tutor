// Generated by scripts/sync-blog-posts.mjs
// Do not edit this file directly. Edit markdown files in content/blog instead.

const postContent = "<p><strong>Enterprise integration • API resilience • MuleSoft architecture</strong></p>\n<p>A timeout protects one request. A retry gives a selected transient failure another chance. A circuit breaker protects the wider platform from repeatedly calling a dependency that is already unhealthy.</p>\n<p>These patterns solve different problems. Used together carefully, they contain failures and preserve capacity. Used without limits, especially through aggressive retries and poorly aligned timeouts, they can turn a slow backend into a wider outage.</p>\n<h2>The Production Problem</h2>\n<p>Consider a backend service that normally responds in two or three seconds. Under load, the same operation begins taking more than 20 seconds.</p>\n<p>The calling API waits, reaches its timeout, and retries. More requests arrive while the first attempts are still consuming resources. Users also repeat actions because they do not know whether the original request succeeded.</p>\n<p>The result can be worse than the original slowdown:</p>\n<ul><li>open requests consume threads, connections, memory, and worker capacity</li><li>connection pools and queues begin to fill</li><li>retries multiply traffic to the struggling dependency</li><li>write operations can create duplicate reservations, payments, or updates</li><li>one slow backend starts affecting unrelated APIs and customer journeys</li></ul>\n<p>The answer is not simply to increase the timeout or retry every failure. Resilient design starts by assigning each control a specific responsibility.</p>\n<h2>Timeout vs Retry vs Circuit Breaker</h2>\n<table><thead><tr><th>Pattern</th><th>Question it answers</th><th>Primary protection</th></tr></thead><tbody><tr><td>Timeout</td><td>How long should this request wait?</td><td>The individual request and caller resources</td></tr><tr><td>Retry</td><td>Should this selected temporary failure be attempted again?</td><td>Recovery from short-lived transient failures</td></tr><tr><td>Circuit breaker</td><td>Should we continue calling this dependency?</td><td>The wider platform and the unhealthy dependency</td></tr></tbody></table>\n<p>The most important distinction is scope. A timeout evaluates one attempt. A retry initiates another attempt. A circuit breaker evaluates recent outcomes across many calls and can stop new attempts temporarily.</p>\n<h2>1. Timeout: Limit How Long One Attempt Can Wait</h2>\n<p>A timeout places a deadline on a downstream operation. Without one, a caller can wait indefinitely while holding resources needed by other work.</p>\n<p>Common timeout types include:</p>\n<ul><li><strong>Connection timeout:</strong> how long to wait while establishing a connection</li><li><strong>Read or response timeout:</strong> how long to wait for data after a connection exists</li><li><strong>End-to-end deadline:</strong> the maximum time available for the complete request across all layers</li></ul>\n<p>A timeout does not make the backend healthy. It only stops the current attempt from waiting longer.</p>\n<h3>Design timeouts as a budget</h3>\n<p>Timeouts should be coordinated across the complete call chain. If a mobile client allows 12 seconds, an Experience API cannot safely spend all 12 seconds waiting for a Process API, which then waits another 12 seconds for a System API.</p>\n<p>Each layer needs enough time to:</p>\n<ol><li>call its dependency</li><li>receive or generate a controlled failure</li><li>perform any permitted retry</li><li>return before its own caller gives up</li></ol>\n<p>Choose values from observed latency percentiles, network overhead, transaction criticality, and the user-facing deadline. A timeout copied from another API is not evidence-based configuration.</p>\n<h2>2. Retry: Repeat Only Failures That May Recover</h2>\n<p>A retry is useful when a failure is temporary and another attempt has a reasonable chance of succeeding. Examples can include a brief network interruption, a connection reset, throttling that provides a retry window, or a short-lived service-unavailable response.</p>\n<p>Retries become dangerous when they are broad, immediate, or repeated at multiple layers. If 100 failed requests each receive three additional attempts, the dependency can receive 300 extra calls while already unhealthy. If several layers retry independently, the multiplication can be much larger.</p>\n<p>A controlled retry policy should define:</p>\n<ul><li>which errors are genuinely transient</li><li>the maximum number of attempts</li><li>the delay between attempts</li><li>exponential backoff and jitter where supported</li><li>the total request deadline</li><li>whether the operation is safe to repeat</li><li>which single layer owns the retry</li></ul>\n<blockquote><p><strong>Retry rule:</strong> never retry because an error merely looks technical. Retry only when the failure classification, operation semantics, and remaining deadline make another attempt safe and useful.</p></blockquote>\n<h3>Why write operations require special care</h3>\n<p>Retrying a read is generally less risky than retrying a request that creates a reservation, charges a card, submits an order, or updates a customer record.</p>\n<p>A timeout proves only that the caller did not receive a response. It does not prove that the backend failed to complete the operation.</p>\n<p>Before retrying a write, use at least one reliable duplicate-prevention strategy:</p>\n<ul><li>an idempotency key</li><li>a unique business reference</li><li>a status-check endpoint</li><li>an upsert or naturally idempotent operation</li><li>an asynchronous acceptance pattern with a correlation ID</li></ul>\n<p>Without such protection, a successful first attempt followed by a lost response can become a duplicate transaction after retry.</p>\n<h2>3. Circuit Breaker: Stop Calling a Dependency That Is Unlikely to Recover Immediately</h2>\n<p>A circuit breaker observes outcomes across multiple calls. When failures or slow calls cross a configured threshold, it temporarily blocks calls to the affected dependency.</p>\n<p>The standard states are:</p>\n<ul><li><strong>Closed:</strong> calls are allowed and outcomes are monitored</li><li><strong>Open:</strong> calls fail fast or use a valid fallback without invoking the dependency</li><li><strong>Half-open:</strong> a limited number of trial calls test whether the dependency has recovered</li></ul>\n<p>Unlike a timeout, which protects one request, a circuit breaker prevents the platform from repeatedly spending resources on a dependency that is already showing sustained failure.</p>\n<p>The breaker should usually evaluate technical health signals such as timeouts, connection failures, service unavailability, and slow-call rates. Business validation errors such as invalid input or a rejected booking do not normally mean the dependency is unhealthy.</p>\n<h2>4. How the Three Patterns Work Together</h2>\n<p>A typical request path is:</p>\n<ol><li>Check whether the circuit currently permits the call.</li><li>Call the dependency with a defined timeout.</li><li>If a selected transient failure occurs, confirm that the operation is safe to repeat.</li><li>Retry a limited number of times using backoff and jitter, while remaining inside the end-to-end deadline.</li><li>Record success, failure, timeout, and slow-call outcomes.</li><li>Open the circuit when recent outcomes cross the configured threshold.</li><li>While open, fail fast or use a truthful, business-approved fallback.</li><li>After the recovery interval, allow a small number of half-open trial calls.</li></ol>\n<pre><code class=\"language-text\">if circuit.isOpen():\n    return controlledUnavailableResponse()\n\nresult = callDependency(timeout = requestBudget)\n\nif result.isTransientFailure()\n   and operation.isSafeToRetry()\n   and deadline.hasTimeRemaining():\n    wait(backoffWithJitter)\n    result = retryOnce()\n\ncircuit.record(result)\nreturn mapToConsumerResponse(result)</code></pre>\n<p>This is conceptual pseudocode. The exact implementation depends on the runtime, connector, service mesh, gateway, and resilience library used by your platform.</p>\n<h2>5. Practical Starting Configuration</h2>\n<p>Suppose a reservation backend normally responds in two to three seconds but sometimes exceeds 20 seconds during peak load. An initial test configuration might be:</p>\n<table><thead><tr><th>Setting</th><th>Illustrative starting value</th></tr></thead><tbody><tr><td>Backend response timeout</td><td>10 seconds</td></tr><tr><td>Maximum retries</td><td>1</td></tr><tr><td>Retry delay</td><td>Exponential backoff with jitter</td></tr><tr><td>Minimum calls before circuit evaluation</td><td>20</td></tr><tr><td>Failure-rate threshold</td><td>50%</td></tr><tr><td>Slow-call threshold</td><td>8 seconds</td></tr><tr><td>Slow-call rate threshold</td><td>60%</td></tr><tr><td>Circuit open duration</td><td>60 seconds</td></tr><tr><td>Half-open trial calls</td><td>3</td></tr></tbody></table>\n<p>These are examples, not universal defaults. Tune them with production latency percentiles, traffic volume, backend capacity, expected recovery time, transaction risk, and acceptable customer experience.</p>\n<p>Test the policy under load. A setting that appears reasonable during a single manual request may behave very differently when hundreds of callers retry together.</p>\n<h2>6. Mule 4 Implementation Notes</h2>\n<p>Mule 4 provides the <code>Until Successful</code> scope for synchronous retry behavior. MuleSoft documents that it retries all processors inside the scope until they succeed or the configured retry count is exhausted.</p>\n<p>That behavior creates two important design responsibilities:</p>\n<ul><li>Keep the scope small so a retry does not repeat unrelated processors.</li><li>Do not place unsafe write operations inside it unless duplicate execution is controlled.</li></ul>\n<pre><code class=\"language-xml\">&lt;until-successful\n    maxRetries=&quot;${reservation.retry.max}&quot;\n    millisBetweenRetries=&quot;${reservation.retry.interval}&quot;&gt;\n    &lt;flow-ref name=&quot;call-reservation-backend&quot; /&gt;\n&lt;/until-successful&gt;</code></pre>\n<p>This example shows retry structure only. Error selection, timeout configuration, idempotency, backoff strategy, circuit state, and observability still need deliberate design.</p>\n<blockquote><p><strong>MuleSoft warning:</strong> <code>Until Successful</code> is a retry mechanism, not a complete circuit-breaker strategy. Wrapping a backend call in this scope does not by itself prevent repeated calls during a sustained outage.</p></blockquote>\n<h2>7. Where the Logic Belongs in API-Led Architecture</h2>\n<p>Backend-specific resilience usually belongs close to the dependency. Business recovery decisions belong where the business process is understood.</p>\n<table><thead><tr><th>Layer</th><th>Typical responsibility</th></tr></thead><tbody><tr><td>System API</td><td>Backend timeout, selected technical retry, circuit state, and error translation</td></tr><tr><td>Process API</td><td>Business fallback, orchestration, compensation, or asynchronous handling</td></tr><tr><td>Experience API</td><td>Channel-appropriate response for web, mobile, or partner consumers</td></tr><tr><td>API gateway / ingress</td><td>Rate limits, spike control, quotas, and traffic protection</td></tr></tbody></table>\n<p>This is a useful default, not an absolute rule. The final design should prevent multiple layers from retrying the same failure independently.</p>\n<h2>8. Choose Fallbacks That Tell the Truth</h2>\n<h3>For read operations</h3>\n<ul><li>return valid cached data</li><li>return the last known result with a clear freshness indicator</li><li>return partial information when the missing dependency is non-critical</li><li>fail fast with a clear temporary-unavailability response</li></ul>\n<h3>For write operations</h3>\n<ul><li>fail fast and ask the caller to retry later</li><li>return <code>202 Accepted</code> only when the request was safely accepted for asynchronous processing</li><li>queue the operation only when business rules permit delayed execution</li><li>return a correlation ID and provide a status endpoint</li></ul>\n<p>Never return fake success for an unconfirmed write. A fallback should preserve business truth, not merely improve the HTTP success rate.</p>\n<h2>9. Common Design Mistakes</h2>\n<h3>Retrying every 5xx response</h3>\n<p>Not every server error is transient. Repeating a persistent failure can intensify an outage.</p>\n<h3>Retrying at every API layer</h3>\n<p>Retries multiply across the call chain. Assign retry ownership to one deliberate point.</p>\n<h3>Retrying non-idempotent writes</h3>\n<p>A blind retry can create duplicate payments, reservations, orders, or updates.</p>\n<h3>Using the same timeout everywhere</h3>\n<p>A content lookup, CRM search, payment authorization, and reservation creation flow have different latency and risk profiles.</p>\n<h3>Opening the circuit for business errors</h3>\n<p>Invalid input, duplicate data, or a failed business rule does not automatically indicate an unhealthy dependency.</p>\n<h3>Continuing after the caller's deadline</h3>\n<p>Work that continues after the consumer has timed out can waste capacity or complete a transaction the user believes failed.</p>\n<h3>Returning fake success</h3>\n<p>Do not claim that a write succeeded unless completion was confirmed or the request was safely accepted for later processing.</p>\n<h2>10. Monitoring and Operational Visibility</h2>\n<p>Resilience controls must be observable. Operations teams should know whether retries are helping, when a circuit opens, and whether the dependency recovers.</p>\n<p>Track:</p>\n<ul><li>timeout count and rate</li><li>retry count and retry success rate</li><li>circuit state changes and open duration</li><li>slow-call rate and backend latency percentiles</li><li>fallback usage</li><li>half-open trial outcomes</li><li>duplicate-prevention and idempotency outcomes</li><li>remaining deadline at each attempt</li></ul>\n<p>Logs and traces should include the correlation ID, API, dependency, operation, elapsed time, retry attempt, circuit state, and final outcome. Never log complete credentials, payment data, or sensitive payloads to gain observability.</p>\n<h2>Production Review Checklist</h2>\n<ul><li>Define the end-to-end deadline before setting individual timeouts.</li><li>Confirm which errors are transient and retryable.</li><li>Assign retry ownership to one layer.</li><li>Limit attempts and use backoff with jitter where supported.</li><li>Protect write operations with idempotency or a status-check pattern.</li><li>Keep circuit-breaker thresholds specific to the dependency and operation.</li><li>Exclude business validation failures from technical health calculations.</li><li>Use truthful fallbacks.</li><li>Test slow, failed, partially recovered, and overloaded scenarios.</li><li>Alert on rising retries, circuit transitions, and fallback usage.</li><li>Revisit thresholds using production evidence rather than leaving initial values unchanged.</li></ul>\n<h2>Final Lesson</h2>\n<p>Timeout, retry, and circuit breaker are complementary controls:</p>\n<ul><li><strong>Timeout</strong> contains one slow attempt.</li><li><strong>Retry</strong> handles a small set of temporary failures.</li><li><strong>Circuit breaker</strong> protects the platform during sustained failure.</li></ul>\n<p>The objective is not to prevent every failure. It is to contain failure, preserve capacity, recover safely, and give consumers a controlled and truthful response.</p>\n<p>For another request-path example, read <a href=\"/blog/integration/mulesoft-client-id-secret-401-nginx-ingress\">When MuleSoft Client ID Enforcement Returns 401 Behind NGINX Ingress</a>, or explore more <a href=\"/blog/integration\">Integration engineering guides</a>.</p>\n<h2>Official References</h2>\n<ul><li><a href=\"https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/\">AWS Builders' Library: Timeouts, retries, and backoff with jitter</a></li><li><a href=\"https://learn.microsoft.com/en-us/azure/architecture/patterns/circuit-breaker\">Microsoft Azure Architecture Center: Circuit Breaker pattern</a></li><li><a href=\"https://docs.mulesoft.com/mule-runtime/latest/until-successful-scope\">MuleSoft Documentation: Until Successful Scope</a></li></ul>"

export default postContent
